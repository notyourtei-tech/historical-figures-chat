# 贡献指南

感谢你愿意让万古灵犀变得更准确、更好用、更包容。对话体验、人物档案、无障碍、性能、测试和文档都欢迎改进。

## 开始前

1. 先阅读 [README](README.md) 和 [架构说明](docs/architecture.md)。
2. 运行 `npm ci`，从 `.env.example` 创建本地 `.env.local`。
3. 保持默认 `HISTORICAL_CHAT_MODE=offline` 完成普通开发和测试；不要提交任何密钥、会话、导出数据或真实用户对话。

## 开发标准

- 使用 TypeScript；避免 `any`、未使用导入和隐式失败。
- 优先复用现有设计令牌、组件和可访问性模式，而不是添加一次性的颜色、间距或交互。
- 保持桌面与 320px–Pixel 7 宽度都没有横向溢出；所有触控操作必须可达，键盘焦点必须可见。
- 任何新的产品事件都必须先加入 `src/lib/analytics-events.ts`，并更新 `docs/measurement-plan.md`；事件绝不能携带聊天正文或个人信息。
- 人物档案要将创意表达与历史事实区分开。不要捏造出处、医疗/法律/金融建议，或把角色扮演当作可引用的史料。

## 提交前检查

```bash
npm run lint
npm run typecheck
npm test -- --runInBand
npm run build
npm run test:e2e
```

改动聊天、登录、隐私、会话或布局时，请在 PR 描述中说明：桌面与移动端的验证结果、键盘路径、隐私影响和回退行为。

## Pull Request 流程

1. 一个 PR 只解决一个清晰的问题。
2. 描述用户可见的变化与不包含的范围。
3. 新增或更新能够证明修复的测试；修复回归时先写能失败的测试。
4. 不要混入格式化噪声、生成目录、`.env*` 或无关重构。
5. CI 通过后再请求评审。对于身份、数据删除、内容治理或安全头的改动，至少进行一次额外人工检查。

## 报告漏洞

请不要通过公开 Issue 报告可被利用的安全问题。参见 [SECURITY.md](SECURITY.md)。
